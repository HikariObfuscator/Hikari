language: c++
branches:
  except:
    - master
os:
  - osx
  - linux
compiler: clang
script:
- mkdir Build && cd Build && cmake -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=MinSizeRel -DLLVM_APPEND_VC_REV=on ../ && make -j8
